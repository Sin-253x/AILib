{
  "compilerOptions": {
    "target": "ES2017",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts"
  ],
  // ======================== 代码解释 ========================
  // 1. 整体功能：
  //    控制 Next.js 应用生产构建的 TypeScript 检查范围。
  // 2. 关键部分拆解：
  //    - node_modules：第三方依赖不参与项目源码类型检查。
  //    - e2e：Playwright 端到端测试由独立命令 npm run test:e2e 验证，不混入应用构建。
  // 3. 重要概念与库：
  //    - tsconfig exclude：只影响 TypeScript 项目扫描边界，不会删除或跳过测试文件本身。
  // 4. 潜在问题与改进建议：
  //    - 如果后续新增单元测试目录，建议单独配置测试 tsconfig，避免生产构建边界膨胀。
  // 5. 修改指南：
  //    - 新增非生产源码目录时，先判断它是否应进入 Next.js build，再决定是否加入 exclude。
  // ========================================================
  "exclude": [
    "node_modules",
    "e2e"
  ]
}
