# ======================== 代码解释 ========================
# 1. 整体功能：
#    控制 Next.js Web 镜像的构建上下文，只发送源码和配置文件。
# 2. 关键部分拆解：
#    - 排除 node_modules：容器内会重新安装依赖，不复制 Windows 本地依赖目录。
#    - 排除 .next：容器内会重新构建生产产物，不复用本地开发产物。
#    - 排除测试输出：避免 Playwright 或覆盖率文件污染镜像。
# 3. 重要概念与库：
#    - Docker build context：上下文越小，构建越快，权限问题越少。
#    - Next.js build artifact：.next 是构建结果，不应作为源文件发送。
# 4. 潜在问题与改进建议：
#    - 如果新增静态资源目录，通常不需要排除，确保它能随源码进入镜像。
# 5. 修改指南：
#    - 新增本地缓存目录时，把目录名加入本文件。
# ========================================================
node_modules
.next
out
coverage
playwright-report
test-results
