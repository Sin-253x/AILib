# ======================== 代码解释 ========================
# 1. 整体功能：
#    控制 FastAPI 镜像构建上下文，只发送后端运行所需源码和依赖清单。
# 2. 关键部分拆解：
#    - 排除虚拟环境：容器会在镜像内重新安装依赖，不应复制 Windows 本地 .venv。
#    - 排除测试缓存：避免 .pytest_cache 权限问题或无关缓存污染镜像。
#    - 排除环境变量文件：运行时由 docker-compose 注入变量，不在构建阶段复制密钥。
# 3. 重要概念与库：
#    - 镜像构建阶段：只应包含可复现构建所需文件，运行时配置通过环境变量注入。
#    - Docker secret hygiene：避免把 API Key、数据库密码等写入镜像层。
# 4. 潜在问题与改进建议：
#    - 如果后续新增模型文件或大体积数据，应按需排除或改用卷挂载。
# 5. 修改指南：
#    - 新增后端本地缓存目录时，同步加入本文件，保持构建上下文轻量。
# ========================================================
.env
.env.*
!.env.example

.venv
.pytest_cache
.ruff_cache
__pycache__
**/__pycache__
**/*.pyc

htmlcov
coverage
