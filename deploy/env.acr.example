# ======================== 配置解释 ========================
# 1. 整体功能：
#    为 deploy/docker-compose.acr.yml 提供需要追加到 deploy/.env.prod 的 ACR 镜像变量示例。
#
# 2. 关键部分拆解：
#    - ACR_REGISTRY：阿里云 ACR 区域 Registry，例如 registry.cn-shanghai.aliyuncs.com。
#    - ACR_NAMESPACE：ACR 命名空间，通常是个人或项目名称。
#    - AILIB_IMAGE_TAG：本次部署使用的镜像标签，建议使用 Git commit SHA。
#
# 3. 重要概念与库：
#    - 镜像标签：服务器通过 tag 精确拉取对应版本，避免 latest 指向不明确。
#    - .env.prod：Compose 运行时读取的私有配置文件，不应提交到 Git。
#
# 4. 潜在问题与改进建议：
#    - ACR_NAMESPACE 必须和阿里云 ACR 控制台中创建的命名空间一致。
#    - 如果使用私有仓库，服务器需要先 docker login。
#
# 5. 修改指南：
#    - 在服务器 deploy/.env.prod 末尾追加下面三项，并替换为真实值。
# ========================================================
ACR_REGISTRY=registry.cn-shanghai.aliyuncs.com
ACR_NAMESPACE=your-namespace
AILIB_IMAGE_TAG=latest
